# Expenses

Bank CSV import, auto-categorization, and receipt scanning.

## Key Functions

- `app.importer` — CSV import from banks
- `app.categorizer` — auto-categorization rules
- `app.receipts` — receipt OCR via Tesseract
- `app.bank_feed` — Plaid bank feed integration
