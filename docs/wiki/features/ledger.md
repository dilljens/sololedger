# Core Ledger

The accounting engine built on Beancount.

## Key Functions

- `app.ledger` — ledger operations and queries
- `ledger/` — Beancount ledger files
- `app.api` — REST API endpoints

## Testing

Run via `python -m pytest` (see pyproject.toml).
